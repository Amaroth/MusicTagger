<Window x:Class="MusicTagger.GUI.StringInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Input" SizeToContent="WidthAndHeight" WindowStartupLocation="CenterScreen"
        ContentRendered="Window_ContentRendered">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Name="QuestionTextBlock" TextWrapping="Wrap" Margin="0,0,0,5" Width="300">Question:</TextBlock>
        <TextBox Name="AnswerTextBox" Grid.Row="1" Width="300">Answer</TextBox>

        <WrapPanel Grid.Row="2" HorizontalAlignment="Right" Margin="0,5,0,0">
            <Button IsDefault="True" Name="OKButton" Click="OKButton_Click" MinWidth="60" Margin="0,0,10,0">_Ok</Button>
            <Button IsCancel="True" MinWidth="60">_Cancel</Button>
        </WrapPanel>
    </Grid>
</Window>
